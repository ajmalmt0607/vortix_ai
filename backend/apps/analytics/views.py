# Actual DRF views live under api/v1/dashboard/ and api/v1/analytics/, matching
# this project's existing API-versioning convention (see api/v1/health/).
# Reserved for future app-level views if needed.
